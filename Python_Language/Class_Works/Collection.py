"""
Collection : which is contain multiple data at same location.

there are 4 types of Collection.

1) List : list is a collection data type which is contain similar and dis-similar data elements at single location
          
          list which is represent by []

          list which is mutable (we can change it value)

2) Tuple : tuple is a collection data type which is represent by ()
   
           tuple which immutable (un-changeble)

3) Set : set is a collection data type which is represent by {}
  
         which is contain only unique values - unorder , unindex

4)  Dictinory : which is represent by {}

    which is contain data in key and value format.
           
"""

